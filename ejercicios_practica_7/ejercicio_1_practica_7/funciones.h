//Ejercicio (Previo): Práctica 7


/*Escriba un fichero (funciones.h) que contenga las declaraciones de las diferentes funciones que se utilizan en el programa principal para resolver el ejercicio en cuestión.*/


bool IsPrime (int number);


void PrimeLower (int number);


bool IsPalindrome (std:: string text);


int factorial (int number);


double MyExp (double exponent, unsigned num_terms);


int DayMonth (int number, int year);


bool LeapYear (unsigned int year);


bool IsPalindromeNumber (int number);


